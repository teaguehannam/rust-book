let
	By default, declares an immutable object
	"let mut" will allow the variable to be reassigned

const (constant)
	naming convention is to use all uppercase with underscores
	valid for entire time a program runs, within declared scope
	useful when multiple parts of program need to know about it
	more info: https://doc.rust-lang.org/reference/const_eval.html
